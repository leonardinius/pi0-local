# Plan: anon-hello-world

## Why

Create `/tmp/hello.txt` now as a one-time anonymous task artifact so the requested file exists with the exact confirmed content.

## What

Create or overwrite `/tmp/hello.txt` so it contains exactly the bytes:

```text
hello world
```

Definition of done: `/tmp/hello.txt` exists and exact byte comparison against `hello world` passes.

## Acceptance criteria

- `/tmp/hello.txt` exists.
- `/tmp/hello.txt` contains exactly `hello world`.
- Exact content means 11 bytes with no trailing newline.
- Exact verification passes with:
  ```sh
  printf %s 'hello world' | cmp -s - /tmp/hello.txt
  ```
- Test/lint/build suite status: N/A because there is no git repository, codebase, or test suite for this one-time task.
- No tests are added or updated, so fail-before/pass-after confirmation is N/A.

## Steps

1. Create or overwrite `/tmp/hello.txt` with exact content:
   ```sh
   printf %s 'hello world' > /tmp/hello.txt
   ```
2. Verify `/tmp/hello.txt` exists. `(depends_on: 1)`
   ```sh
   test -f /tmp/hello.txt
   ```
3. Verify exact byte content. `(depends_on: 1)`
   ```sh
   printf %s 'hello world' | cmp -s - /tmp/hello.txt
   ```
4. Confirm test/lint/build are not applicable because no repository or codebase exists. `(depends_on: 1, 2, 3)`

## Progress

- [x] Step 1: Create or overwrite `/tmp/hello.txt` with exact content
- [x] Step 2: Verify `/tmp/hello.txt` exists
- [x] Step 3: Verify exact byte content
- [x] Step 4: Confirm test/lint/build are not applicable

## Dependencies

No other plans in `doing/` are dependencies for this one-time task.

Runtime dependencies:
- Writable `/tmp` directory.
- Permission to create or overwrite `/tmp/hello.txt`.
- Shell utilities `printf`, `test`, and `cmp` available.

## Constraints / non-goals

- Use the user-approved plan name `anon-hello-world` because there is no git repository basename to derive a project name from.
- Overwriting an existing `/tmp/hello.txt` is acceptable.
- Do not modify any git repository; none is involved.
- Do not create unrelated files or scripts.
- Do not add a trailing newline to `/tmp/hello.txt` unless the requirement changes.
- Advisor recommendation accepted: use `printf` and `cmp` instead of `echo` or `cat` for exact no-newline content handling.
