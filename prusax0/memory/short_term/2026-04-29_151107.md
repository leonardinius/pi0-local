# Session Snapshot — 2026-04-29 15:11 UTC

## Active Task
- User requested a compact `/checkpoint` snapshot only; no long-term promotion.

## Key Decisions
- Installed `leonardinius/pi-telegram` locally from `/home/agent/work/pi-telegram` using `pi install /home/agent/work/pi-telegram`.
- Set `PI_TELEGRAM_AUTOSTART=0` in interactive shell config so Telegram polling does not auto-connect by default; systemd remains the explicit always-on startup path.

## Files Touched / Read
- Modified: `/home/agent/.bashrc` — added `export PI_TELEGRAM_AUTOSTART=0` with systemd note.
- Read: `/home/agent/work/pi-telegram/README.md`, `package.json`, `index.ts`, `lib/runtime.ts`.
- Read: `/etc/systemd/system/work-pi.service`, `/home/agent/work/agent/bin/work-pi-run`, `~/.ssh/config`, `~/.profile`.

## Useful Findings
- `pi-telegram` local install command from README: `pi install /absolute/path/to/pi-telegram`.
- Deploy key aliases in `~/.ssh/config` (`github.com-pi-telegram-deploy`, `github.com-leonardinius-pi-telegram`) authenticate successfully for `leonardinius/pi-telegram`.
- `work-pi.service` starts `/home/agent/work/agent/bin/work-pi-run`, which runs `tmux ... "exec pi /telegram-connect"`, so systemd explicitly connects Telegram.

## Blockers / Next Step
- Push from `~/.pi/agent` failed earlier due SSH permission for its remote; `/home/agent/work/pi-telegram` remote was fixed to deploy-key SSH and was already up to date.
