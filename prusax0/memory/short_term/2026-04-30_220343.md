# Session Snapshot — 2026-04-30 22:03:43 UTC

## Active Task
- Довести стабильный `/projects` publish-flow: shared cloudflared tunnel, корректный Caddy ingress, basic auth per app, и Telegram UX для статусов/паролей/delete.

## Key Decisions
- Откат на модель `1 shared tunnel` с именем `pi-dev0-tunnel`.
- Публичные hostnames зафиксированы как `<app>-<base>` (например, `test1-apps.it101.org`).
- Источник public URL/credentials перенесён в `.env` (`APP_PUBLIC_URL`, `APP_BASIC_AUTH_USER`, `APP_BASIC_AUTH_PASS`), docker labels больше не используются.
- Caddy ingress для published app включает `basicauth`; user берётся из `.env` (обычно `pidev0`).
- В Telegram: кнопка `🔑` отдаёт пароль отдельным сообщением, delete не обновляет верхнюю карточку.

## Files Touched / Read
- Modified:
  - `/home/agent/work/agent/bin/project`
  - `/home/agent/work/pi-telegram/scripts/sync-ingress.ts`
  - `/home/agent/work/pi-telegram/lib/projects.ts`
  - `/home/agent/work/pi-telegram/index.ts`
  - `/etc/caddy/Caddyfile`
  - `/etc/cloudflared/config.yml`
  - `/etc/systemd/system/cloudflared.service`
- Inspected:
  - `/etc/caddy/Caddyfile.projects`
  - `/home/agent/.pi/agent/projects.json`
  - app env files under `/home/agent/work/projects/*/.env`

## Useful Findings
- `530/1033` и `404` с Cloudflare обычно указывали на mismatch tunnel route/ingress config, а не на docker app.
- Caddy catch-all health block может перехватывать host-трафик и ломать expected upstream response.
- Некорректный `APP_PUBLIC_URL` в `.env` (например `https://test1-/`) напрямую ломает generated host rules.
- Для проверки publish важно сверять пары `no auth -> 401` и `with auth -> 200` на public URL.

## Blockers / Next Step
- Проверить end-to-end в Telegram после последних UX-правок (особенно delete flow и password button).
- При необходимости закоммитить последние изменения в `pi-telegram` и отдельно синхронизировать внешние host-level конфиги (`agent/bin/project` находится вне git-репо `pi-telegram`).
